# Remaining Laptop, EV-Folder, and Cloud Tasks

Updated: 21 July 2026

This file contains only work that remains on the development laptop, in the
EV repository, or in the Supabase, EMQX, GitHub, and Cloudflare consoles.
Raspberry Pi installation and physical testing are tracked separately in
[RemaindingRaspberryPi.md](RemaindingRaspberryPi.md). Completed Pi work is
recorded in [DoneRaspberrypi.md](DoneRaspberrypi.md).

A laptop/cloud step remains in this file until every required checkbox and its
pass evidence are complete. Never move laptop/cloud work into the Pi-only
`DoneRaspberrypi.md`; implementation or unit tests alone do not complete a
deployment or hardware gate.

Never paste passwords, API secrets, private addresses, private keys, or exact
device identifiers into this file, Git, screenshots, SQL results, or logs.

## Current starting point

- The Cloudflare frontend is already deployed.
- The Raspberry Pi bridge is deployed as `pi-agent-1.3.0`.
- The historical Step 13.2 controlled command handoff is recorded as done.
- The reviewed implementation snapshot is committed and pushed through
  `4f77166dc7e78ff366ec5c71435d034b44fa2594`.
- Local validation Steps L1-L3 passed on 21 July 2026, including the
  Docker-backed Supabase/Edge Function integration suite and GitHub CI.
- The pending cloud migrations/functions and hardened EMQX path are not yet
  deployed and verified as one current stack.

Complete the following steps in order. Do not deploy a later step when an
earlier gate has failed.

## Phase A — Validate and release the current working tree

### Step L1 — Review and protect the working tree

- [x] Preserve all user changes and inspect modified, staged, and untracked
  files.
- [x] Confirm generated bytecode, local environment files, certificates, and
  credentials are ignored.
- [x] Confirm every staged deletion or new file is intentional.

```powershell
git status --short --branch
git diff --stat
git diff --cached --stat
git diff --check
```

Pass evidence:

- Completed 21 July 2026: `git diff --check` exited successfully.
- The staged Python bytecode deletion is intentional generated-file cleanup;
  Python caches and Supabase CLI runtime directories are ignored.
- No private credential, local environment file, certificate, or generated
  runtime file is selected for commit.
- Modified, staged, and untracked paths were reviewed without discarding the
  user's existing changes.

### Step L2 — Run the full local validation gate

Start Docker Desktop first. The integration runner uses an isolated local
Supabase work directory and refuses a non-loopback Supabase URL.

```powershell
docker version
npm ci
npm run check
npm run test:unit
npm run test:pi
npm run test:esp32
npm run test:emqx-publish
npm run build:vite
npm run test:integration
```

Pass evidence:

- Completed 21 July 2026: migrations `001` through `011` applied cleanly to an
  isolated database, and schema lint returned no errors.
- All 8 pgTAP files passed (191 assertions), including explicit frontend and
  Edge Function API-privilege checks.
- The local ingestion function passed all 14 HTTP EMQX contract scenarios,
  including authentication, identity, schema, ordering, replay, stale-state,
  command reservation, and publish reconciliation.
- Frontend unit tests passed (12), Pi tests passed (27), ESP32 protocol tests
  passed (4), and the EMQX publish-response test passed (1).
- TypeScript checking, Python bytecode compilation, and the Vite production
  build passed.
- The runner removed its temporary work directory and left zero local
  `miit-rover-integration` containers.

### Step L3 — Commit and push the tested snapshot

- [x] Review every candidate path and stage only the explicit manifest.
- [x] Commit and push the tested snapshot to `origin/main`.
- [x] Wait for every required GitHub Actions job to complete successfully.

Commands used:

```powershell
git status --short
git diff --name-status
git ls-files --others --exclude-standard
git add -- <reviewed-paths>
git diff --cached --check
git diff --cached
git commit -m "Harden robot ingestion and command delivery"
git push origin main
```

Pass evidence:

- Completed 21 July 2026. The reviewed snapshot was committed as
  `3f43786d2a729a8b30752befd5102b89025d3253`; the supported-Node CI correction
  produced the final implementation commit
  `4f77166dc7e78ff366ec5c71435d034b44fa2594`.
- The pushed commits contain no detected secret or generated runtime data.
- [GitHub Actions CI #5](https://github.com/RavenPai/EV/actions/runs/29852261556)
  completed successfully for the frontend, Raspberry Pi/ESP32,
  EMQX-response, and isolated Supabase/EMQX checks.
- The Node.js 24 validation repeated 12 frontend tests, TypeScript checking,
  the production build, 1 EMQX publish-response test, all 191 pgTAP
  assertions, and all 14 webhook contract scenarios successfully.
- The committed Pi bundle comparison hashes are recorded in Step P1. The live
  recheck and safe checkout reconciliation intentionally remain Pi Step P1.

Pi dependency: after this step, complete Step P1 in
`RemaindingRaspberryPi.md` before a future Pi pull.

## Phase B — Deploy the current Supabase and EMQX stack

### Step L4 — Back up and inspect the hosted database migration plan

- [x] Confirm the CLI is linked to the intended Supabase project without
  printing credentials.
- [x] Create a recoverable hosted database backup or confirm the project's
  available point-in-time recovery before schema changes.
- [x] Run the active-command collision queries below. Every query must return
  zero rows before the new unique indexes are applied.

```sql
select robot_id, count(*)
from public.robot_commands
where command_type = 'START_MISSION'
  and status in ('PENDING', 'PUBLISH_UNKNOWN', 'PUBLISHED', 'ACKNOWLEDGED')
group by robot_id
having count(*) > 1;

select delivery_id, count(*)
from public.robot_commands
where command_type = 'START_MISSION'
  and delivery_id is not null
  and status in ('PENDING', 'PUBLISH_UNKNOWN', 'PUBLISHED', 'ACKNOWLEDGED')
group by delivery_id
having count(*) > 1;

select robot_id, count(*)
from public.robot_commands
where command_type in ('PAUSE', 'RESUME', 'RETURN_HOME')
  and (
    status in ('PENDING', 'PUBLISH_UNKNOWN', 'PUBLISHED', 'ACKNOWLEDGED')
    or (status = 'COMPLETED' and not coalesce(result, '{}'::jsonb)
      @> '{"consumed": true}'::jsonb)
  )
group by robot_id
having count(*) > 1;
```

Inspect the planned migration set:

```powershell
npx supabase migration list --linked
npx supabase db push --linked --dry-run
```

Pass evidence: the dry run contains only the migrations genuinely absent
from the hosted project. Stop if it proposes an unexpected migration.

Status 2026-07-22: complete from the EV folder.

- Linked project confirmed as `nkvmpjznvkqmgcposaup` (`RavenPai's EV`)
  without printing database passwords or service-role keys.
- Hosted schema dump and public-data dump were written under `.local-backups/`,
  which is Git-ignored.
- All three active-command collision checks returned `0`.
- `npx supabase db push --linked --dry-run` showed only
  `202607210011_robot_ingestion_safety_followup.sql` pending.

### Step L5 — Apply migrations and deploy both Edge Functions

In the Supabase dashboard, set or correct these function secrets without
placing their values in shell history or Git:

- `EMQX_API_URL`
- `EMQX_API_KEY`
- `EMQX_API_SECRET`
- `ROBOT_INGEST_SECRET`

The EMQX API key and secret must be Deployment API credentials, not the
robot's MQTT login.

```powershell
npx supabase db push --linked
npx supabase functions deploy dispatch-delivery
npx supabase functions deploy ingest-robot-message --no-verify-jwt
```

Pass evidence:

- Hosted migration history includes migrations `010` and `011`.
- Both Edge Functions show successful current deployments.
- `mark-stale-robots-offline` and `expire-stale-robot-commands` are scheduled.
- No function secret is present in frontend assets, Git, screenshots, or logs.

Status 2026-07-22: complete.

- Migration `202607210011_robot_ingestion_safety_followup.sql` was applied to
  the hosted project.
- Hosted migration history now includes `202607210010` and `202607210011`.
- `dispatch-delivery` is deployed and active with JWT verification enabled.
- `ingest-robot-message` is deployed and active with JWT verification disabled.
- Supabase secret names exist for `EMQX_API_URL`, `EMQX_API_KEY`,
  `EMQX_API_SECRET`, and `ROBOT_INGEST_SECRET`; only hashed secret fingerprints
  were printed by the CLI.
- `expire-stale-robot-commands` and `mark-stale-robots-offline` are scheduled
  once per minute.

### Step L6 — Repair and verify EMQX command publication

- [ ] Create or verify a least-privilege EMQX Deployment API credential.
- [ ] Confirm the API base URL resolves to the deployment publish endpoint.
- [ ] Create a clearly labelled, isolated non-physical test robot, subscriber,
  credentials, and delivery for exhaustive publish-result testing.
- [ ] Confirm the physical Pi subscriber separately before any truthful,
  motor-disconnected robot check.
- [ ] Inspect the API response and the resulting database transaction.

Never make the physical robot appear ready by fabricating `ONLINE`/`IDLE` or
healthy LiDAR, camera, or ESP32 telemetry. A real web `START_MISSION` is deferred
to Step P22, after the actual state producer and sensors pass.

Pass evidence:

- HTTP `200` is the only broker response classified as delivered. The command
  becomes `PUBLISHED`, or remains at a valid later `ACKNOWLEDGED`/`COMPLETED`
  state when robot evidence wins the race; the delivery advances to
  `DISPATCHED` or beyond.
- HTTP `202` with `no_matching_subscribers` records `FAILED` and leaves the
  delivery `ASSIGNED`.
- Timeout, 5xx, or an unknown response remains `PUBLISH_UNKNOWN` and is
  reconciled instead of blindly retried.
- The earlier HTTP `403` condition no longer occurs.
- The isolated test identity remains tightly restricted through Steps P3–P5
  and L9, then is disabled or removed after L9 evidence collection.

Status 2026-07-22: not complete from the EV folder.

- The deployed function and required Supabase secret names are present, but the
  live publish path still needs an authenticated staff dispatch test.
- The EV folder does not contain staff test-account credentials, a service-role
  key, or EMQX Deployment API credentials that can be safely used for the
  isolated broker-result checks.
- Do not use the robot MQTT username/password as the `EMQX_API_KEY` and
  `EMQX_API_SECRET`; L6 requires Deployment API App ID/App Secret credentials.

### Step L7 — Repair and verify the EMQX-to-Supabase HTTP action

Attach one action to a rule covering these topics:

- `miit/robots/+/acks`
- `miit/robots/+/state`
- `miit/robots/+/events`
- `miit/robots/+/presence`

Use the maintained rule contract:

```sql
SELECT
  bin2hexstr(id) AS mqttMessageId,
  topic,
  payload,
  clientid,
  username,
  qos,
  timestamp
FROM
  "miit/robots/+/acks",
  "miit/robots/+/state",
  "miit/robots/+/events",
  "miit/robots/+/presence"
```

The HTTP body must contain the complete envelope documented in `README.md`,
and `x-emqx-secret` must equal the Supabase ingestion secret. Configure
non-2xx retry/backoff and inspect connector/action metrics.

Pass evidence for this configuration checkpoint:

- The connector is healthy and uses the intended HTTPS endpoint.
- The SQL rule test produces every required envelope field for all four topic
  branches.
- The action is linked to the rule and maps the complete JSON body plus the
  protected header without exposing its value.
- A controlled configuration test has no connector, TLS, template, or action
  error.

Close Step L7 at this configuration checkpoint, then complete L8 and Pi Steps
P3–P5. Their live validation, presence, event, idempotency, and ordering evidence
is evaluated with the isolated state/ACK regression in Step L9.

Status 2026-07-22: not complete from the EV folder.

- The hosted ingestion function is deployed and its shared-secret name exists.
- The EV folder cannot verify or repair the EMQX Cloud HTTP Server connector,
  rule action, retry settings, or action metrics without EMQX console/API
  access.
- The current hosted robot rows still show stale/offline bridge timestamps; no
  fresh broker-to-Supabase presence was observed during this EV-folder pass.

### Step L8 — Enforce least-privilege EMQX authorization

For each robot identity:

- Allow subscribe only to that robot's `commands` topic.
- Allow publish only to that robot's `acks`, `state`, `events`, and
  `presence` topics.
- Deny every unmatched operation.
- Use a separate temporary publisher for command tests; never authorize the
  robot identity to publish commands.

Pass evidence:

- All intended operations succeed.
- Cross-robot topics, command wildcards, open wildcards, and robot-to-command
  publication fail.
- MQTT device credentials and Deployment API credentials are different.

Status 2026-07-22: not complete from the EV folder.

- Authorization rules must be changed and tested in EMQX Cloud.
- The EV folder has no safe non-interactive access to the broker authorization
  configuration. Do not paste MQTT passwords into shell commands or commit them
  into a local env file.

### Step L9 — Run isolated cloud regression and truthful Pi checks

Depends on Pi Steps P3–P5. Keep motor power disconnected. Step P10 is reserved
for the later real mission-manager path and is not required for the isolated
cloud regression.

Part A — isolated non-physical test robot:

1. Create a delivery as an authenticated normal user.
2. Use the Step L6 test publisher/subscriber to identify this row clearly as
   non-physical test infrastructure and supply its controlled test state.
3. Approve, assign, and dispatch once as verified staff.
4. Verify publication response, simulated handoff, ACK ingestion, and audit
   timestamps.
5. Feed test state and linked events through the real broker/webhook.
6. Verify delivery advancement occurs only from robot events.
7. Exercise duplicate, expired, malformed, wrong-robot, bad-secret, forbidden
   topic, stale-state, replay-conflict, and impossible-transition cases.
8. Disable or remove the temporary identity/credentials after retaining
   redacted evidence.

Part B — physical Pi truth check:

1. Verify retained presence and controlled offline/reconnect behavior.
2. Retain P3's broker-observed validation/rejection logs without issuing a new
   physical mission/control command.
3. Publish only actual safe test events. Defer complete physical state ingestion
   to P14, when all required values have real producers.
4. Defer physical audited command/ACK and real web dispatch/`START_MISSION` to
   Step P22.

Useful read-only checks:

```sql
select status, published_at, acknowledged_at, result
from public.robot_commands
order by created_at desc
limit 10;

select status, bridge_online, bridge_last_seen,
       last_seen, telemetry_received_at, telemetry_at
from public.robots
where id in ('<TEST_ROBOT_ID>', '<PHYSICAL_ROBOT_ID>');

select robot_id, delivery_id, command_id, event_type, severity, occurred_at
from public.robot_events
order by occurred_at desc
limit 20;
```

Pass evidence:

- One redacted test packet links the isolated delivery, command, simulated
  handoff, ACK, state, events, and expected transitions.
- Presence advances `bridge_last_seen`; fresh state advances only the intended
  robot's telemetry timestamps; ACK updates only its matching command; and one
  valid event produces one row.
- Identical retries remain idempotent, conflicting reuse is rejected, and
  invalid ordering cannot advance delivery.
- A separate packet records the physical Pi's truthful presence, subscription,
  P3 rejection evidence, and actual events without fabricated sensor health.
- The temporary identity and credentials are disabled or removed after the
  final evidence is retained.

Status 2026-07-22: not complete.

- L9 depends on L6, L7, L8, and Pi Steps P3-P5.
- The hosted database and Edge Functions are ready for this test, but the EV
  folder lacks the authenticated normal-user/staff test credentials and the
  temporary EMQX isolated test identity needed to run the full workflow.
- Physical Pi truth checks must use real retained broker/Supabase evidence and
  must not fabricate online, idle, sensor, ACK, or mission-state data.

### Step L10 — Redeploy and smoke-test the Cloudflare frontend

Deploy only after the hosted backend is compatible:

```powershell
npm run build:cloudflare
npm run deploy:cloudflare
```

Pass evidence:

- Authentication and direct React routes work at the production URL.
- Delivery creation, notifications, fleet state, and dispatch errors use the
  current backend behavior.
- Built assets contain no service-role or EMQX credential.

## Phase C — Complete remaining application safeguards

### Step L11 — Add server-enforced rate limiting and abuse protection

- [ ] Move delivery creation behind a protected RPC or Edge Function.
- [ ] Revoke browser direct-insert permission after the protected path works.
- [ ] Add transaction-safe per-user delivery limits.
- [ ] Add per-user and per-robot command limits inside command functions.
- [ ] Return HTTP `429` with an appropriate retry interval.
- [ ] Add concurrency, boundary, bypass, and separate-user tests.
- [ ] If Turnstile is used, validate its token server-side; do not rely on the
  browser widget alone.

Pass evidence: concurrent requests cannot bypass the limits, while normal
independent users remain unaffected.

### Step L12 — Add operational alerts for EMQX failure and backlog

Monitor at least:

- HTTP action failures and retries.
- Connector disconnection.
- Persistent action backlog.
- Repeated command-publication failures.
- Old `PUBLISH_UNKNOWN` commands.
- Messages accepted by EMQX but absent from Supabase.

Send alerts to an operator-controlled channel without including payloads or
secrets.

Pass evidence: an isolated simulated failure generates one actionable alert,
recovery clears it, and routine robot-offline events do not cause an alert
storm.

### Step L13 — Add atomic cross-delivery robot reservation

- [ ] Add a database RPC that locks the delivery and robot, verifies readiness,
  and prevents two active deliveries from reserving the same robot.
- [ ] Reject races that assign one robot or delivery twice.
- [ ] Add pgTAP concurrency and authorization tests.

Pass evidence: concurrent attempts to assign different deliveries to one robot
produce exactly one success and no double reservation.

### Step L14 — Implement the cargo confirmation lifecycle

- [ ] Define lock hardware and the local controller interface.
- [ ] Add the required database schema/RPCs and server-side APIs.
- [ ] Generate short-lived, single-use confirmation codes and store only
  protected one-way representations.
- [ ] Bind each code to one delivery and one lifecycle stage.
- [ ] Consume confirmation and advance the allowed state in one transaction.
- [ ] Enforce user/staff/robot authorization, retry limits, rate limits, audit
  history, expiry, and staff recovery.
- [ ] Implement the robot-local cargo interface and a hardware-free simulator.
- [ ] Add SMS/email/push only through a server-side provider with privacy and
  delivery-failure handling.
- [ ] Add database, API, local-controller, replay, and concurrency tests.

Pass evidence: the complete server/local flow passes; replay, guessing,
cross-delivery use, concurrent consumption, and expired codes fail without a
partial state change.

Pi cargo installation, bench verification, and supervised mission: Steps
P23–P24.

## Phase D — Build remaining robot software in the EV repository

These tasks are written and tested on the laptop, then installed and proven
on the Pi using the matching `P*` steps.

### Step L15 — Implement the persistent mission manager

- [ ] Add `robot-pi/mission_manager.py` and pure state-machine modules.
- [ ] Consume current `command-inbox/{commandId}.json` files, never the
  archived guide's single `mission_request.json` design.
- [ ] Give ESTOP and PAUSE deterministic priority.
- [ ] Persist active mission and last-consumed command before side effects.
- [ ] Preserve each command's `issuedAt` and `expiresAt` in its inbox record and
  re-check freshness immediately before every mission or control side effect.
- [ ] Implement reboot recovery without repeating motion, events, or cargo
  actions.
- [ ] Atomically write fresh `robot_state.json` and durable linked events.
- [ ] Implement locally checked RESUME and the linked `RESUMED` event.
- [ ] Cover every state, transition, duplicate, crash point, and safety fault
  with hardware-free tests.

Pi deployment: Step P10.

### Step L16 — Implement one local serial-owner/base-controller interface

- [ ] Choose one process as the only ESP32 serial owner.
- [ ] Route cloud PAUSE/ESTOP and navigation velocity through protected local
  IPC.
- [ ] Implement boot sessions, monotonic sequence, CRC, TTL, ACK/NACK
  matching, state/fault parsing, reconnect, and safe shutdown.
- [ ] Add tests for stale session/sequence, bad CRC, expired frames, timeout,
  disconnect, and process death.

Pi deployment: serial preflight Step P2, base-controller Step P8, and
motor-disconnected protocol campaign Step P9. Raised-wheel verification is
Step P16.

### Step L17 — Finish production ESP32 firmware

The checked-in v0.2 package is commissioning firmware, not final unattended
motor firmware.

- [ ] Confirm the real ESP32 board and motor/safety pin map.
- [ ] Add encoder feedback, per-wheel closed-loop PID, odometry measurements,
  plausibility/stall checks, and supported electrical/thermal telemetry.
- [ ] Enforce local velocity and acceleration limits.
- [ ] Keep STOP-after-boot, watchdog, heartbeat, TTL, CRC, ESTOP latch, and
  physical-reset requirements.
- [ ] Disable legacy unauthenticated Bluetooth for production or redesign it
  with an explicit reviewed safety boundary.
- [ ] Compile in CI or a documented local toolchain and add deterministic
  firmware/protocol tests.

Pi flashing and motor-disconnected verification: Steps P6–P9. Raised-wheel
verification is Step P16.

### Step L18 — Create navigation, sensor, and map configuration

- [ ] Choose the navigation architecture: ROS 2/Nav2 for the full rover or a
  constrained route controller for a supervised prototype.
- [ ] Create pinned, versioned configurations for LiDAR, camera, encoder/IMU
  odometry, TF frames, footprint, limits, localization, and navigation.
- [ ] Create the validated schema and templates that will map cloud `loc-*` IDs
  to map-frame poses and a map checksum/version. Do not invent final poses
  before the physical map is produced in Step P19.
- [ ] Add simulation and configuration-validation tests.

The Pi runs Ubuntu 26.04 arm64. Do not copy the archived guide's Ubuntu 24.04
Jazzy commands. Recheck the official ROS installation page at execution time;
the current compatible release documentation is:

https://docs.ros.org/en/lyrical/Installation/Alternatives/Ubuntu-Install-Binary.html

Initial Pi installation and commissioning: Steps P11–P19.

### Step L19 — Validate and release robot-side source before Pi installation

This is a second release gate for source created in Steps L14–L18; the earlier
Step L3 release does not cover future code.

- [ ] Re-run the Step L1 review and all relevant Step L2 checks.
- [ ] Compile the exact ESP32 firmware target, retain the immutable binary
  artifact, and validate generated robot configuration without including
  credentials or machine-local paths.
- [ ] Commit and push the reviewed mission-manager, serial-controller,
  firmware, navigation, and robot-facing cargo changes.
- [ ] Wait for CI and record the immutable commit, firmware source and binary
  hashes, configuration hash, dependency versions, and compatible protocol
  version.
- [ ] Build a deployment manifest so each Pi step can prove exactly which
  reviewed component it installed.

Pass evidence: CI passes and every artifact intended for P6, P8, P10, P11, or
P23 is traceable to the immutable tested commit and recorded hash.

### Step L20 — Import and release the measured map and real location poses

Run this step only after Pi Step P19 produces a measured, validated map.

- [ ] Transfer the approved map artifacts to the repository without copying
  credentials, runtime logs, or unrelated Pi files.
- [ ] Record the map checksum/version and measured map-frame pose for every
  supported cloud location ID.
- [ ] Reject missing IDs, duplicate mappings, invalid quaternions, out-of-map
  poses, and checksum mismatches.
- [ ] Re-run simulation and configuration validation against the measured map.
- [ ] Commit/push the map/configuration update, wait for CI, and deploy any
  matching server-side location data through the reviewed migration path.

Pass evidence: the laptop, backend, mission manager, and Pi navigation stack
refer to one immutable map version and the same validated location mappings.

## Phase E — Final laptop-side acceptance

### Step L21 — Complete security, load, and release evidence

Run after Pi Steps P22 and P24 provide the no-cargo and cargo
hardware-in-the-loop evidence. This step consumes those results; it does not
wait for Step P25.

- [ ] Re-run RLS and role-escalation tests.
- [ ] Verify normal users cannot access other users' deliveries.
- [ ] Verify only current verified staff can approve, assign, or command.
- [ ] Run load/concurrency tests for delivery, dispatch, ingestion, and
  notification paths.
- [ ] Run dependency, secret, and generated-asset scans.
- [ ] Define database/event retention, account registration, credential
  rotation, incident response, and rollback procedures.
- [ ] Attach the hardware-in-the-loop and supervised-route evidence from Pi
  Steps P22–P24 before declaring a campus pilot ready.

Pi Step P25 begins only after this laptop gate passes.

Pass evidence: a tagged release points to the tested commit, migration set,
function versions, frontend deployment, Pi bundle hash, firmware hash, and
signed acceptance results.
